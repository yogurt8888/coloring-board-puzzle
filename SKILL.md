---
name: coloring-board-puzzle
description: 求解「画盘」类染色棋盘小游戏关卡（给定一张游戏截图，算最短通关步骤并输出统一格式的通关路线图）。核心机制：选画笔颜色点某一格，该格所在的整块连通同色区变成画笔色。关键认知——区域被并大后再点同一格，染的是「长大了的那块」，所以最优解几乎总是「一直点同一格、轮换颜色」。自带 1~21 关答案索引：用户重复发已解关卡或直接报「第 8 关」时秒回。当用户发来画盘/染色棋盘截图并问"这关怎么过"、或直接说"第N关/画盘N 怎么解"时使用。触发词：画盘、染色小游戏、全部颜色格恢复为、剩余步数、画笔染色、how to pass this level。
agent_created: true
---

# 画盘类染色棋盘 —— 从截图到通关路线图

一个纯 Python 求解器 + 21 关答案索引。任何 agent（Claude / GPT / 豆包 / WorkBuddy / Cursor …）
只要会跑命令行就能用：**发一张游戏截图，或直接说"第 8 关怎么过"**，输出一段结论 + 一张
`画盘N_通关路线.png`。

> **给调用方的模型建议**：「直接发截图」这条路需要模型自己能看图（认出棋盘配色、目标色、
> 「剩余步数」提示），请优先使用带识图能力的多模态模型，例如 **hy4**、
> **DeepSeek V4.0 PRO / V4.1 FLASH**、**GLM 5.3 / 5.3 FLASH** 等。
> 纯文本模型读不到图，只能走「用户直接报关卡号」的入口（`--level N`），
> 或让用户用文字描述目标色与剩余步数（`--target` / `--budget`）。

## 玩法机制（模式）

选一个画笔颜色 → 点棋盘上某一格 → **该格所在的整块连通同色区**全部变成画笔色。
目标：全盘变成指定颜色，在给定步数内完成。

## 最关键的一条认知（几乎人人卡在这里）

**区域被并大之后，再点"同一格"，染的就是那块「长大了的区域」，而不是原来那一小块。**

所以最优解几乎总是：**选定一格，然后一直点它，只轮换画笔颜色**。
如果以为每步要换格子点，就会觉得"7 个色块怎么只给 4 步"、"步数根本不够"。
反过来，如果看到「非目标色块数 > 剩余步数」，不要急着下结论说无解 —— 先试「一直点同一格」。

等价理解：一直点同一格 ≡ 从该格做洪水填充（Flood-It）。

## 第一步永远是这条：先选入口

| 用户给了什么 | 走哪条路 | 命令 |
|---|---|---|
| **只说了关号**：「第 8 关怎么过」「画盘 9 的解法」 | ① 查已解索引（不重算） | `pipeline.py --level 8 --out .` |
| **发了截图** | ② 标定取色 → 自动比对索引：命中就复用，未命中就完整求解 | 见下方标准命令 |
| 棋盘文件已知 / 只想重出一张图 | ③ 直接给棋盘 | `pipeline.py --board board8.txt --level 8 --out .` |

索引里有**第 1~21 关**（`assets/levels/levels.json` + 同名成品图），所以：

- 用户把已经解过的关**再发一次截图** → **秒回**（直接取成品图，约 0.05 秒），不要重算；
- 但复用判据很严：**63 格逐格相同 且 目标色相同**，差一格都不复用 ——
  这类关卡长得像的很多，差一格结论就完全不同。不命中时流水线会自动重新求解，
  并在简报里给一句"与第 N 关只差 k 格"的提示；
- 想强制重算：加 `--no-cache`。

想知道索引里有哪些关：`python scripts/pipeline.py --list-levels`

**本地查不到这一关时，脚本会先联网同步一次再下结论 —— 别急着说"没解过这一关"：**

- 用户问「第 14 关」而本地索引里没有 → 自动取远端 `assets/levels/levels.json`（约 4 KB），
  版本更新就合并写回本地、补齐缺失的成品图，然后照常秒回；
  简报里会多一行「索引更新：已同步远端索引 v…（本次从远端同步，已写回本地）」；
- 远端也没有 / 断网 → 静默降级，照常走本地索引或完整求解；标准输出里会给一行
  「远端同步：…」说明原因，此时再按"这关没解过"处理（要图求解）；
- 手动拉全量：`python scripts/pipeline.py --sync-levels`；
- 用户明确要离线：`--no-sync`，或环境变量 `CB_OFFLINE=1`；
- 只访问 `raw.githubusercontent.com`，只写 `assets/levels/` 一个目录。

## 安装与依赖

```bash
git clone https://github.com/yogurt8888/coloring-board-puzzle.git && cd coloring-board-puzzle
pip install -r requirements.txt      # 只有一个依赖：Pillow
python tools/selfcheck.py            # 自检：环境 / 字体 / 索引 / 求解链路
python scripts/pipeline.py --level 8 --out /tmp/out
```

- Python ≥ 3.8，Windows / macOS / Linux 通用，无编译依赖。
- **中文字体随仓库自带**（`assets/fonts/CBBoardSans-*.ttf`，由 Noto Sans SC 子集化并按 OFL 改名，
  许可见 `assets/fonts/OFL.txt`），所以不必预装字体，也不会画出方块。
  想换字体：环境变量 `CB_FONT_REGULAR` / `CB_FONT_BOLD` 指向任意 `.ttf`/`.ttc`。

## 标准工作流

**默认一条命令跑完，并把结论一起打回来**：`pipeline.py` 从截图一路跑到交付图 + 一页简报。

```bash
cd "<工作区>" && python "scripts/pipeline.py" \
    --img "<截图>" --level 13 --target G --budget 5 \
    --expect-cols 9 --expect-rows 7 \
    --out "<工作区>" && cat "<工作区>/_pipeline/h13/pipeline_summary.txt"
```

**这条命令的形态本身就是为省往返设计的**（目标：发图后 2 分钟内给答案）：

1. **把「跑脚本」和「读结论」并进一次调用**：`... pipeline.py ... && cat summary.txt`。
   只要你的执行工具会回显 stdout，就一次拿全；若不会（例如某些 Windows 工具只回 exit code），
   就让 `--out` 落在本地、再读一次那个 `pipeline_summary.txt`。
2. **多张图同样一次调用**：`for` 循环跑完再用一条 `grep -E "推荐打法|颜色顺序"` 把所有结论打全，
   别一张图一条命令。
3. **别一条条手动跑下面那些脚本** —— 那样单关 6~8 轮"等输出再决定下一步"，实测 5~10 分钟，
   而脚本本身只占 1~3 秒（索引命中时 0.05 秒）。**瓶颈从来是往返次数，不是算力。**

按这个姿势，单关的完整链路是 **2 次工具调用**（跑并读结论 → 把图给用户看），脚本墙钟 1~3 秒。
若用户没在文字里说清**目标色**和**剩余步数**（这两个都只能从图上读），才需要多一次图片查看。

产出：

| 文件 | 用途 |
|---|---|
| `<out>/画盘<level>_通关路线.png` | **只交这一张** |
| `<out>/_pipeline/h<level>/pipeline_summary.txt` | 一页结论（推荐路线 / 三道校验 / 耗时）——**读它就能写回复** |
| `<out>/_pipeline/h<level>/board<level>.txt` | 取色得到的棋盘，留档复用 |
| `<out>/_pipeline/h<level>/grid_recon.png` | 原图 vs 重建，核对取色 |

`--img` 与 `--board` 二选一（后者用于"棋盘已知 / 只想重出图"）。建议一律带上
`--expect-cols 9 --expect-rows 7`：标定偏一行/一列时取色会全错却**不会自己报错**，这道校验专门拦它。
其余常用：`--sub`（交付图副标题；`--foot` 默认不画，别传）、`--cols`（面板列数）、`--work`（中间目录）。

流水线内部三道校验，任一不过**立即中止**并写明卡在哪一步：

| 校验 | 做什么 | 不过意味着 |
|---|---|---|
| 双实现对账 | `solve_fast` 与 `crosscheck` 两套独立实现，算出的「步数 / 可点格数」必须相同 | 有一套错了，必须查清再交付 |
| 最短性证明 | BFS 只穷举 `≤ L-1` 层，确认没有更短的走法 | 同格法不是最优，需改走通用 BFS 解 |
| 成品反查 | 从交付图 PNG 反查每帧盘面 + 圈 | 画出来的与算出来的不符 |

降级路径：自动标定失败 → 手工 `scan` + `extract` 拿到棋盘 → 用 `--board` 再跑流水线。

### 底层脚本（流水线内部就是调它们；需要手工干预时才单独用）

| 脚本 | 作用 | 是否交付物 |
|---|---|---|
| `scripts/pipeline.py` | **一键流水线**：标定→求解→复核→出图→反查，写一页简报；也负责查索引 | 否（内部） |
| `scripts/level_index.py` | 已解关卡索引：`--level` 直查、截图指纹匹配、命中即复用 | 否（内部） |
| `scripts/board_tool.py` | `auto`/`scan`/`extract` 标定格线并逐格取色 | 否（内部） |
| `scripts/solve_fast.py` | 首选求解器（组件图状态 BFS + 同格法枚举） | 否（内部） |
| `scripts/crosscheck.py` | 格子级独立复核器，**交付前必跑** | 否（内部） |
| `scripts/render_delivery.py` | **统一格式交付图**（用户指定版式） | **是，只交这一张** |
| `scripts/verify_render.py` | 从交付图 PNG 反查每帧盘面**和圈**，**出图后必跑** | 否（内部） |
| `scripts/solve.py` | 旧对照实现，仅留档 | 否 |

### 1. 读棋盘：别靠眼睛数格子


背景通常是暖色渐变（本身就是高饱和），**按饱和度找棋盘会被背景污染**。可靠做法：

```powershell
& "<python>" "<skill>/scripts/board_tool.py" auto --img "<截图路径>" --out "<工作目录>"
```

脚本会自动定位格线、逐格取色分类，并输出：

| 文件 | 用途 |
|---|---|
| `grid_recon.png` | 原图裁剪 vs 程序重建的并排图 —— **必须人眼看这张图核对** |
| `grid_report.txt` | 格线标定参数 + 逐格颜色矩阵 + 每格平均 RGB + **标定过程日志** |

自动标定的做法（**v4，别改回去**）：

1. 用 `_tile_chain` 在扫描线里挑「最长等宽等距瓦片链」，拿到格距和种子行列中心；
2. **种子自检**：种子行/列必须自己就通过「像棋盘行」的校验，不过直接判失败；
3. 再沿四个方向逐格扩展，扩展判据 = `_is_board_line`。

`_is_board_line` 的判据（v4 重写，**与"缝隙是什么颜色、有多宽"完全无关**）：
先把这一条上的「瓦片色段」取出来（3 点投票抗噪 + 丢掉 <18px 的碎段），再用「≤0.45 格距」
的间距把它们并成"棋盘带"，取最长的那条带，要求：

- ① 带长 ≥ 3.2 格；
- ② 带里**还能分辨出的独立格子数 ≥ 0.75 × 带长/格距**。

- 判据 ② 是关键：标题栏 / 立绘 / 卡片凑出来的"一整块纯色"可能很长，但里面分不出格子。
  v3 只要求"段数够 + 3/4 的间距贴着格距"，在**底色干净**的图上会被一段纯色背景骗过 ——
  实测合成图上标题右侧的背景段 `(222,200,176)` 被当成一行，整块棋盘上移一格标成 9x8、
  取色 72 格全错却**一路不报错**；v4 的"带里要能分辨出格子"把它排掉了。
- ⚠️ **别用"合并窄缝"去处理瓦片内高光**（我试过，退回了）：瓦片内部有一条 1~10px 的竖向
  高光带（近白，既不像瓦片色也不像缝隙色），会把一格切成两三段；但**缝里也散落 1px 杂点**，
  按"窄缝合并"会顺着杂点把两道真缝桥接起来，整行并成一格（第 3 关实测 x=792 处
  `(735,737,743,746)` 四个 1px 杂点就这样把 17px 的真缝桥掉了）。正确做法是丢掉碎段后
  **容忍多出来的段** —— 判据 ② 写的是"≥0.75×n_cells"而不是"=="，天然容忍一格被切成两三段。
- **实测三套底色都适用**：第 1~3 关的缝是米色 `(207,174,145)`、宽 15~24px；第 8~12 关的缝近白、
  宽约 20px；合成测试图的缝只有 4px 纯白。v3 那种「段后面必须紧跟近白留白（min>185）」的写法
  在米色缝的关卡上会把**所有**候选段全否掉（第 1 关实测：`find_line` 候选 0 个 → 标定直接失败）。
- 第 2 条防的是同类错误的另一种形态：**种子落到棋盘外时，扩展照样能成功**。
  静默给出错棋盘是最危险的失败，宁可标定失败转人工 `scan`。
- **仍然必须扩展**：棋盘最后一格的右边是外框而不是缝，纯靠"数链上几段"必然漏掉最右一列；
  老版本就因此把第 11 关判成 8 列、第 12 关直接失败。
- ⚠️ **参数命名与直觉相反，改代码前先看清**：`_tile_runs(img, horizontal=True, fixed=x)`
  扫的是**竖线**（沿 y 走，用来判这一列有没有 7 行）；`horizontal=False, fixed=y` 扫的是**横线**
  （沿 x 走，判这一行有没有 9 列）。写诊断脚本时非常容易搞反，实测为此多花了半小时。

若自动标定仍不准（`grid_report.txt` 里会写失败原因），退到手工标定：

```powershell
& "<python>" "<script>/board_tool.py" scan --img "<截图>" --out "<目录>" --y 580 --y 900 --x 1080
```

⚠️ `scan` 的 `--out` **也是必填**。`scan` 会沿指定行/列做游程（RLE）扫描，把每个长色段的起止 x/y
写进 `grid_scan.txt` —— 相邻格中心距就是格距，首格边缘就是原点。
拿到 `x0 / 格宽 / y0 / 格高 / 列数 / 行数` 后用 `extract` 模式重取：

```powershell
& "<python>" "<script>/board_tool.py" extract --img "<截图>" --x0 660.9 --px 96.125 --y0 246 --py 96 --cols 9 --rows 7 --out "<dir>"
```

**判据：每个格子的平均 RGB 应当精确落在有限几种标准色上**（如红 252,139,120 / 黄 242,221,97 /
蓝 166,212,251 / 绿 92,235,148）。如果出现一堆既不像红也不像黄的中间色，说明网格没对准，重新标定。
`grid_report.txt` 里的「无法归类(=?)的格子数」应当为 0。

**然后必须做一次逐格取色复核**：把原图每格中心色与棋盘矩阵比对，应当 **63/63 全吻合**；
有不符就把那一格标红另存并且别默默交付。判据就是上面那组标准色，四色最小距离通常 <12，很容易判定。

### 2. 求解

**首选 `solve_fast.py`**（组件图状态）：

```powershell
& "<python>" "<skill>/scripts/solve_fast.py" --board "<board文件>" --out "<输出目录>" --colors RYGB --max-depth 6
```

`--board` / `--out` 都是必填；`--colors` 不传则从棋盘里去重推断，但**推断会漏掉"棋盘上没出现、
但画笔面板里有"的颜色**，按游戏画笔面板显式传入更保险。`--max-depth` 默认 6。

**默认走快路径（v3）**：先分层枚举「一直点同一格」求最短步数 `L`（一到 `L` 就停，不必枚举更深），
再让 BFS **只穷举 `≤ L-1` 层**来证明没有更短的走法。结论与"完整 BFS"完全等价，
但省掉了原本最贵的那一层（最深一层状态数最多，而它只是为了"找解"——可我们已经拿到解了）。
加 `--full` 才回到"完整 BFS + 列出全部最短解"的慢路径。

实测第 8 关（5 步、17 个初始块）：`solve_fast` 15.4s → **1.3s**；第 11 关 5.4s → 1.0s。
9 关整体（求解 + 复核）38.4s → 14.1s，且**9 关的步数 / 推荐格 / 颜色顺序 / 每步染色格数与旧版逐字一致**。

`board` 文件格式：每行一串颜色字母，**最后一行是目标色**，例如画盘-10（技能自带示例见 `examples/`）：
```
RGGGBBBBB
RYYRRRRRR
RYBBBGGGR
RYBBBBBGY
RYBBBBBRY
RYYYRRRRY
RRBBBBBBB
R
```

脚本输出：
1. 初始连通块清单 + 非目标色块个数
2. **最短步数**（逐层 BFS + 全局去重，故必为最优）与**全部最短解**（含"不同步点不同格"的）
3. **「一直点同一格」能通关的点击格清单**，按颜色顺序聚合、列出全部可用格子
4. 一张**内部用**的 `solve_route.png`（黑圈标点击格）。**这不是交付物** ——
   给用户的那张图走 `render_delivery.py`（见 §3），版式是用户指定的统一格式。

把最短步数和截图上的「剩余步数」对一下：**相等 = 这条解就是设计者要的最优解**（第 10~12 关都是相等）。

> **为什么不能直接用 `solve.py`（整盘字符串状态）**：
> 它的状态是 63 个字符的整盘串，每次转移都要重跑一遍连通块分析。
> 深度 4 时第 10 关约 2 万态还凑合，**深度 5 直接跑不出来**（第 11/12 关实测卡死）。
> `solve_fast.py` 用的是这条不变量：
> **染色永远不会把一块拆开** → 一个当前连通块 = 若干初始连通块的并集
> → 每块初始块在任意时刻都是单色的 → 局面只需用「每块初始块当前什么颜色」表示。
> 状态数上限从 4^63 降到 4^(初始块数)（本项目约 10~14 块），每次转移只动连通块图上几个节点。
> 一条安全剪枝：只考虑「棋盘上当前存在的颜色」（点到场上没有的颜色不可能并块，纯浪费一步）。
> `solve.py` 保留作对照实现，别删。

### 2b. 独立复核（交付前必做，别只信一个脚本）

```powershell
& "<python>" "<skill>/scripts/crosscheck.py" --board "<board文件>" --colors RYGB
```

`crosscheck.py` 是**格子级**的独立实现：不建连通块图、不压缩状态，
每步对整张 63 格棋盘做一次洪水填充，枚举 `itertools.product(colors, repeat=L)`。
报告写到 board 同目录的 `crosscheck_report.txt`。

**两边的"同格法最短步数"和"可用点击格总数"必须完全一致**，不一致就是有一边错了。
本项目靠这一步抓到过两个真 bug：
1. 旧 `solve.py` 对每个可点格只报第一条颜色顺序 → 漏掉了另外两种同样成立的顺序；
2. `solve_fast.py` 的 `fixed_cell` 曾只收 `ss[0]` → 第 11 关少报了 5 种颜色顺序。
两版现在都会列全部顺序。数值对账样例：第 10 关 33 解/19 格、第 11 关 87 解/21 格、第 12 关 67 解/45 格。

复核脚本的设计要点：
- 自己写 `region()` 洪水填充，**不要 import 被复核的模块**，否则等于没复核。
- **跳过"画笔颜色 == 该格当前颜色"的序列**（空操作，不算有效步）。
- **两处纯剪枝（v3 加的，严格等价，只省算力）**：首色 == 该格起始色（第 1 步必是空操作）、
  相邻两步同色（第 2 步该格已是前一颜色）。实测砍掉约 3/4 候选，耗时 2.7s → 1.5s，
  且 9 关的「最短解条数 / 覆盖可点格数」与剪枝前逐字一致。
- 输出按「点击格」和「按颜色顺序」两个维度聚合，方便挑"对用户最宽容"的那条。

> 这两个复核/校验脚本都已抽出可直接调用的函数：`crosscheck.solve_same_cell(grid, target, colors, max_len)`
> 与 `verify_render.verify(png, grid, target, tap, seq, cols)`，模块底部都有 `__main__` 保护，
> **可以安全 import**（`crosscheck.py` 早期没有保护，import 会直接跑 `main()`，已修）。
> 流水线就是靠它们把 5 个脚本并成一个进程，省掉 4 次解释器启动。

### 3. 交付给用户时（**统一格式，只出一张图**）

用户已指定统一交付格式，**不要每次自己排版**，直接调用技能里的渲染器：

```powershell
& "<python>" "<skill>/scripts/render_delivery.py" `
    --board "<board文件>" --tap 2,5 --seq RBG `
    --level 4 --budget 3 --out "<工作区根目录>" `
    --sub  "一句话心法"
```

（**平时不要传 `--foot`** —— 底部那行说明已按用户要求取消，默认不画。）

产出 `画盘{level}_通关路线.png`，版式固定为：

```
[画盘-4]   ← 深色胶囊底 + 白字，26 号粗体（用户要求「保留、加大、更显眼」）
最优解： 3 步  （预算 3 步，刚好用满）
一句话心法
┌ 第1步 | 选【红】 ┐  ┌ 第2步 | 选【蓝】 ┐
│ 盘面（本步动手前）│  │      ...         │
│ 圈出要点的格子    │  │                  │
│ 点第 2 行第 5 格  │  │ 再点同一格(2,5)   │
│ 这一步把 9 格染成红│  │…把 39 格染成蓝…  │
└──────────────────┘  └──────────────────┘
（画布到此为止 —— **已经没有底部说明行了**）
```

格式要点（这些都是用户的明确要求，改版前先问）：
- **浅色底 + 深色字**（不是深色底），整体接近白纸。
- 顶部关卡标签 `画盘-N`：**深色胶囊底 + 白字、字号 26 粗体**。用户 2026-09-18 明确说
  "不用去掉、加大字号、更显眼一点" —— **不要删掉它，也不要缩回原来的小灰字**。
- **底部没有补充说明行**：用户 2026-09-18 明确要求去掉（原来自动生成的那句
  「注意：非目标色块 11 块 > 预算 5 步，逐块硬刷走不完，必须先并块。」）。
  渲染器已改为**默认不画**，只有显式传 `--foot` 时才画。
- 标题固定为 `最优解：N 步（预算 M 步，富余 K 步）`，`N 步` 放在浅蓝圆角框里；
  `--budget` 就是截图上的「剩余步数」；N == M 时显示「刚好用满」。
- 面板数 = 步数，**不画"初始局面"和"终局"额外帧**。每个面板展示的是**这一步动手之前**的盘面。
- 每步要点的格子用圆圈标出，**圈的颜色 = 本步画笔色**（不是固定色），圈要略大于一格。
- 每步头部：`第 k 步 | 选【色】`，前面一条该画笔色的竖色条，色名用该画笔色写。
- 每步两行说明：第一行说"点第几行第几格"，第二行说"染了多少格 / 比上一步多几格"，
  最后一步以"全盘 N 格变X，通关"收尾。
- 面板列数：步数 ≤4 用 2 列，≥5 用 3 列（`--cols` 可强制）。
- 说明文字超出面板宽度会自动换行，面板高度随行数自适应。
- **副标题会自动按画布宽度折行**（行数计入总高，长文案不用手改）。
  早期版本是单行绘制，长文案会被右边缘直接截断 —— 别再把文案改短去迁就脚本。

常用可选参数：`--cols 2|3`、`--caps "第一行1|第一行2"`、`--notes "第二行1|第二行2"`（都用 `|` 分隔每步）。
渲染器内部**会用同一份棋盘独立跑一遍并校验终局**，`final_ok=False` 会以非零码退出；
脚本还会打印 `sizes=[...]`（每步染色格数），拿去和 `crosscheck_report.txt` 对一下，对不上说明 tap/seq 传错了。

#### 3b. 出图后必须做的「成品反查」

```powershell
& "<python>" "<skill>/scripts/verify_render.py" `
    --png "<工作区>/画盘6_通关路线.png" --board "<board文件>" --tap 3,3 --seq YRBG
```

它从**交付图本身**把每帧面板逐格取色，与"该步动手前应有的盘面"比对，打印逐帧 ✓/✗ 与
`RESULT: PASS/FAIL`，不一致会把差异格的行列和颜色列出来。它校验**两件事**：

1. **盘面**：每帧 63 格逐格一致；
2. **圈**：每帧的圈存在、墨色像素数够、**圈色 = 本步画笔色**、且**圈心落在要点的格子上**（偏移 ≤ 6px）。
   圈是交付图的灵魂（"点哪一格"全靠它传达），只查瓦片颜色是查不出圈画错/画偏的。

棋盘原点**不硬编码**：脚本逐列扫描"瓦片色像素带"自动定位每个面板的棋盘左上角，
所以顶部标题行数、副标题折行数、面板说明行数变化都不会让校验跑偏
（早期版本硬编码 `EXTRA_TOP` 近似值，实测偏 10px，只靠 42px 的格子宽度侥幸没翻车）。

这一步不能省：

- Read 工具看到的是**缩略图**，红/黄在缩小后极易看错。本项目实测踩过两次：第 6 关第 3 帧第 2 行明明
  是红格，肉眼读成了黄格；第 8 关第 1 帧明明是正确的**蓝圈**（底下压着红格），肉眼读成了红圈。
  **像素级反查才是硬证据**；真要围观一处细节，把那处**裁出来放大**再看。
- 顺便验证版面几何没跑偏（面板数、行距都会校验，版面规则不匹配直接 FAIL）。

所以验收是两道：**Read 目视**（看排版：换行、圈位、遮挡）+ **verify_render 反查**（看盘面与圈）。

> 备选（非标准）：`board_tool.py auto` 产生的 `grid_recon.png`（原图 vs 重建并排）是**内部核对用**，
> 不要交付给用户。早期那种"在原截图上画外框+行列号"的标注图已不再作为交付物。

## 已解关卡索引（第 1~21 关）

**索引本体** = `assets/levels/levels.json`（每关的棋盘指纹 + 目标色 + 预算 + 推荐路线）
\+ `assets/levels/画盘N_通关路线.png`（成品图）。用户重复发已解关卡、或只报关号时，**直接取用不重算**。

| 关 | 目标 | 最短 / 预算 | 一直点 | 颜色顺序 | 每步染色 |
|---|---|---|---|---|---|
| 1 | 黄 | 1 / 1 | 第2行第5列 | 黄 | 30 |
| 2 | 绿 | 2 / 2 | 第1行第4列 | 蓝 → 绿 | 28 → 42 |
| 3 | 蓝 | 3 / 3 | 第1行第3列 | 红 → 绿 → 蓝 | 7 → 31 → 47 |
| 4 | 绿 | 3 / 3 | 第2行第1列 | 红 → 蓝 → 绿 | 9 → 39 → 55 |
| 5 | 红 | 4 / 4 | 第3行第9列 | 绿 → 黄 → 蓝 → 红 | 8 → 24 → 43 → 56 |
| 6 | 绿 | 4 / 4 | 第3行第3列 | 黄 → 红 → 蓝 → 绿 | 9 → 23 → 37 → 50 |
| 7 | 红 | 4 / 4 | 第2行第6列 | 蓝 → 黄 → 绿 → 红 | 6 → 11 → 35 → 50 |
| 8 | 蓝 | 5 / 5 | 第1行第8列 | 蓝 → 黄 → 绿 → 红 → 蓝 | 12 → 26 → 35 → 49 → 61 |
| 9 | 蓝 | 4 / 4 | 第1行第4列 | 绿 → 红 → 黄 → 蓝 | 6 → 19 → 31 → 50 |
| 10 | 红 | 4 / 4 | 第2行第4列 | 黄 → 绿 → 蓝 → 红 | 7 → 18 → 25 → 50 |
| 11 | 红 | 5 / 5 | 第3行第8列 | 红 → 蓝 → 黄 → 绿 → 红 | 5 → 22 → 34 → 45 → 58 |
| 12 | 绿 | 5 / 5 | 第1行第8列 | 黄 → 蓝 → 黄 → 红 → 绿 | 6 → 23 → 36 → 54 → 61 |
| 13 | 绿 | 5 / 5 | 第3行第1列 | 蓝 → 黄 → 红 → 蓝 → 绿 | 5 → 15 → 30 → 48 → 57 |
| 14 | 蓝 | 5 / 5 | 第1行第3列 | 红 → 绿 → 黄 → 红 → 蓝 | 7 → 16 → 26 → 43 → 53 |
| 15 | 蓝 | 5 / 5 | 第2行第2列 | 黄 → 绿 → 红 → 黄 → 蓝 | 6 → 17 → 32 → 41 → 53 |
| 16 | 蓝 | 5 / 5 | 第3行第5列 | 蓝 → 绿 → 红 → 黄 → 蓝 | 1 → 4 → 14 → 42 → 50 |
| 17 | 黄 | 5 / 5 | 第2行第1列 | 蓝 → 红 → 蓝 → 绿 → 黄 | 6 → 17 → 30 → 41 → 50 |
| 18 | 黄 | 5 / 5 | 第6行第2列 | 黄 → 绿 → 红 → 蓝 → 黄 | 5 → 22 → 38 → 52 → 63 |
| 19 | 蓝 | 5 / 5 | 第1行第7列 | 蓝 → 黄 → 红 → 绿 → 蓝 | 6 → 17 → 28 → 44 → 57 |
| 20 | 红 | 5 / 5 | 第4行第3列 | 红 → 蓝 → 绿 → 黄 → 红 | 2 → 21 → 33 → 49 → 59 |
| 21 | 红 | 5 / 5 | 第4行第5列 | 绿 → 黄 → 蓝 → 绿 → 红 | 1 → 5 → 15 → 18 → 52 |

> 这张表与 `assets/levels/levels.json` 都由 `tools/build_level_index.py` **从棋盘与求解器现算**，
> 改关卡/改推荐规则后重跑它即可（`--check` 只校验不写）。**不要手工编辑 JSON 或本表** —— 必然与求解器漂移。
> 加新关卡 / 发新版本的完整流程见 `CONTRIBUTING.md`（维护者文档，不必读给用户听）。
> 注意「颜色顺序」里同一个颜色可以出现两次（第 12 关就是 黄→蓝→黄→红→绿），不是笔误。

全部为 9 列 × 7 行 = 63 格、4 色（红 R / 黄 Y / 蓝 B / 绿 G）。行、列均从 1 计。

> **下面每关的「推荐」就是 `recommend()` 的输出**，不要手工另挑一条。它的打分依次看：
> ① 可用点击格数（越多越宽容）→ ② 末步区域大小（越大说明卷进来的格子越多）→
> ③ 最小单步增量（越大越好，避开"某步几乎没长大"的走法）。三者都相同取构造顺序第一条，结果可复现。
> 第 8 关就是靠 ②③ 从两条 12 格路线里挑出 12→26→35→49→61 那条，而不是 11→20→22→46→60。

> 每关的格距都不一样（同一 1920 宽的截图：第 1 关 102.0/101.5、第 2 关 98/98、第 3 关 96/98.5、
> 第 8 关 ~79），**别把某一关的标定值当常数写进代码**；每次都要让 `auto_calibrate` 重新量。

**画盘-1**（目标全黄 Y，限 1 步，最优 1 步）—— 教学关，一眼可解
```
YYYYYYYYY / YYYYRRRRR ×6   目标 Y
```
2 个连通块：第 1 行 9 个黄格与左侧 4 列（第 2~7 行）24 个黄格连成一片 = 33 格黄；右侧 5 列 × 6 行 = 30 格红。
最短 **1 步**：点红块任意一格选【黄】→ 红块 30 格变黄、直接与黄片连通，**63 格全黄**。
可点格 30 个（= 整个红块），块内点哪个都一样。
⚠️ 这关的**第 1 行整排是黄的**（红块只占第 2~7 行），肉眼看缩略图很容易误判成"红块从第 1 行开始"——
取色结果才是准的。

**画盘-2**（目标全绿 G，限 2 步，最优 2 步）—— 三色竖条
```
GGGYYYYBB ×7   目标 G
```
3 个连通块：左 3 列绿 21 格、中 4 列黄 28 格、右 2 列蓝 14 格。
最短 **2 步**：点第 1 行第 4 列（黄块）依次【蓝】→【绿】，每步染 28 → 42 格。
第一步把黄块刷蓝，直接吞掉右侧蓝块（28+14=42）；第二步刷绿，再吞掉左 3 列绿（42+21=63）。
可点格 42 个（2 个初始连通块），本条顺序下 28 个（= 黄块全部，块内点哪个都一样）。

**画盘-3**（目标全蓝 B，限 3 步，最优 3 步）—— 「一条柱子串起四层」
```
RRYRRRRRR ×2 / BBYBBBBBB ×2 / GGYGGGGGG ×2 / RRYRRRRRR   目标 B
```
5 个连通块：第 3 列那根**黄柱 7 格**（唯一纵向贯穿全盘的一列）、第 1~2 行红 16 格、第 7 行红 8 格、
第 3~4 行蓝 16 格、第 5~6 行绿 16 格。
最短 **3 步**：点第 1 行第 3 列（黄柱）依次【红】→【绿】→【蓝】，每步染 7 → 31 → 47 格。
黄柱先染红（7+24=31）→ 再染绿（31+16=47）→ 再染蓝（47+16=63）。
可点格 7 个（1 个初始块），**块内点哪个都一样**。
心法：**先在这根"从头串到尾"的柱子上，把夹在中间的各层逐个并进来**，柱子本身颜色换来换去不要紧。

**画盘-4**（目标全绿 G，限 3 步，最优 3 步）—— 这是「反着染」的教科书例子
```
RRBBBBRRR / GGGGGGGGG / RRBBBBRRR / GGGGGGGGR / RRBBBBRRR / RRRRRRRRR / RRBBBBRRR  目标 G
```
10 个连通块（4 块蓝、4 块红、2 块绿），非目标色块 8 个。最短 **3 步**，且**最短解在块状态空间里唯一**（2 步无解）。
同格法只有 1 种颜色顺序、覆盖 1 个初始块 = **第 2 行整排 9 个格随便点哪个都行**：
**红 → 蓝 → 绿**，每步染 9 → 39 → 55 格。

为什么这么走（关键）：
1. **第一步是「反着染」**——点一个绿格却刷红色。第 2 行那排绿格是唯一同时贴着上方红碎片、下方红碎片的"桥"。
   把它染红后，上下 4 块红碎片 + 底部那块 23 格大红块全部连通，一口气变成 **39 格**的大红块。
2. 第二步刷蓝：这个 39 格红块直接贴着上方两块蓝条（r1/r3 的 c3-6），又通过底部红块贴着下方两块蓝条（r5/r7 的 c3-6）——
   4 条蓝条全被吸进来，变成 **55 格**。
3. 第三步刷绿：这一块只差第 4 行那 8 个绿格没连着，染绿后自动并成一片，**全盘 63 格变绿**。

心法：**先找那个"能同时贴住最多异色碎片"的连通块，第一步反过来把它刷成碎片的颜色，让碎片先连成一片；
后面每一步都在"吸收"贴边的新色块。** 这关如果按直觉先去处理蓝条，3 步绝对打不完。

**画盘-5**（目标全红 R，限 4 步，最优 4 步）—— 「反着染」的第二次出现，比第 4 关更极端
```
YYYYYYYGG / BBBBBGGGG / YYYYYYYYR / GGRRRRRRR / BBBBBBBBG / YYGGGGGGG / YYRRRRRRR  目标 R
```
10 个连通块（2 块红含目标色、8 块非目标色）。最短 **4 步**（3 步无解），与游戏「剩余步数 4」一致。
同格法 2 种颜色顺序、两种顺序覆盖**同一初始块** = 8 个格：(第3行第9列) 与 (第4行第3~9列)，点哪个都行。

| 颜色顺序 | 每步染色格数 |
|---|---|
| 绿 → 黄 → 蓝 → 红 | 8 → 24 → 43 → 56 |
| 绿 → 蓝 → 黄 → 红 | 8 → 24 → 37 → 56 |

> 「每步染色格数」是**本次点击实际改色的格数**，不等于区域大小。末步 56 + 底部 r7c3~c9 那 7 格原生红格 = 63，
> 全盘变红。出图时别把这个 56 当成"没染完"。

为什么这么走：
1. **第一步又是「反着染」**——点的是红格却刷绿。第 3 行第 9 列那块红（8 格，L 形：r3c9 + r4c3~c9）是唯一同时
   贴上上方绿块（6 格）与下方绿块（8 格）的枢纽；染绿后连 r4c1~c2 那 2 个绿格一起并成 **24 格**大绿块。
2. 第二步刷黄：这 24 格立刻贴住上方 7 格黄（r1c1~c7）、左侧 8 格黄（r3c1~c8）、下方 4 格黄（r6c1~c2 + r7c1~c2），
   一并吸进来变成 **43 格**。
3. 第三步刷蓝：43 格 + 原本就蓝的两条（r2c1~c5 共 5 格、r5c1~c8 共 8 格）= 56 格全蓝。
4. 第四步刷红：56 格染红，加上底部 r7c3~c9 那 7 格原生红格，**全盘 63 格变红**。

心法：和第 4 关同一条，但更极端 —— 这关**唯一可行解的第一步就必须"反着染"**，而且 8 个可点格全部落在
同一个红块内。盘面上那两块"已经是目标色"的红块（r3c9+r4c3~c9 与 r7c3~c9）就是设计者埋的钩子：
**目标色的块不一定是"已完成"，可能是那个必须亲手拆掉的枢纽。**

**画盘-6**（目标全绿 G，限 4 步，最优 4 步）—— 起点自由度最大的一关（30 个格可用）
```
GGGGGGGRR / YYYYYBBBR / YBRRRRRBR / YBGGGGRBR / YBGGRRRYR / RBBYYYYYR / RRRBBBRR  目标 G
```
10 个连通块、8 个非目标色块。最短 **4 步**（3 步无解），与游戏「剩余步数 4」一致。
同格法 **72 条解、覆盖 30 个格、6 种颜色顺序**：

| 颜色顺序 | 可点格数 | 起点落在哪个初始块 |
|---|---|---|
| 黄 → 红 → 蓝 → 绿 | 24 | 右上蓝块 / 左侧蓝块 / 中间红块 / 左下红块 |
| 黄 → 蓝 → 红 → 绿 | 24 | 同上 |
| 红 → 黄 → 蓝 → 绿 | 6 | 右下方那片黄（r5c8 + r6c4~c8） |
| 红 → 蓝 → 黄 → 绿 | 6 | 同上 |
| 蓝 → 红 → 黄 → 绿 | 6 | 同上 |
| 蓝 → 黄 → 红 → 绿 | 6 | 同上 |

推荐：**一直点第 3 行第 3 列**（中间那坨红格的左上角），黄 → 红 → 蓝 → 绿，每步染 **9 → 23 → 37 → 50** 格。

1. 第 3 行第 3 列那块红（9 格：r3c3~c7 + r4c7 + r5c5~c7）是**盘面枢纽**：染黄后，左侧那条 8 格竖黄
   （r2c1~c5 + r3c1 + r4c1 + r5c1）与右下方那片 6 格黄（r5c8 + r6c4~c8）一起并成 **23 格**长折线。
2. 染红：折线左下够到 r5c1 → 连上左下角 5 格红（r6c1 + r7c1~c4）；右端够到 r5c8 → 连上右边缘 9 格红，合成 **37 格**。
3. 染蓝：37 格同时贴住三片蓝（右上 5 格、左侧 5 格、底部 3 格）→ **50 格**全蓝。
4. 染绿：50 格 + 原有两片绿（顶部 7 格 + 中间 6 格）= **63 格全绿**。

心法：**先找"能同时贴住最多异色碎片"的枢纽块，第一步把它刷成邻居的颜色，让两片同色先连成一条长折线**；
折线一旦形成，后面每步都能一次够到 2~3 片新色块。
两个坑：① 起点若选在**本来就是黄色**的块上，颜色顺序必须以红或蓝开头（第一步刷黄 = 空操作，不算有效步）；
② 这关"反着染"的味道比第 4、5 关淡 —— 第一步刷的是**邻居的颜色**（黄），不是"目标色换掉"。

**画盘-7**（目标全红 R，限 4 步，最优 4 步）—— 同格法可选起点最多的一关（45 个格）
```
GGGGGYYYY / YYYYYRRRY / YRRGGGGRY / YRYYRRGRY / YRYYYYGRY / YRRBBBBBY / GGGGRRRRR  目标 R
```
11 个连通块、7 个非目标色块。最短 **4 步**（3 步无解），与游戏「剩余步数 4」一致。
同格法 **64 条解、覆盖 45 个格、7 种颜色顺序**：

| 颜色顺序 | 可点格数 | 起点落在哪个初始块 |
|---|---|---|
| 蓝 → 黄 → 绿 → 红 | **22** | 右侧红块 / 左侧红块 / 中间绿块 / 左下绿块 |
| 蓝 → 绿 → 黄 → 红 | 9 | 右上那条 9 格竖黄（r1c6~c9 + 右侧一列） |
| 绿 → 黄 → 蓝 → 红 | 9 | 左侧那条 9 格竖黄（r2c1~c5 + 左下一列） |
| 绿 → 蓝 → 黄 → 红 | 9 | 同上 |
| 红 → 黄 → 绿 → 红 | 5 | 中间蓝杠（r6c4~c8） |
| 黄 → 绿 → 黄 → 红 | 5 | 同上 |
| 绿 → 黄 → 绿 → 红 | 5 | 同上 |

推荐：**一直点第 2 行第 6 列**（右侧那坨红格的左上角），蓝 → 黄 → 绿 → 红，
每步动手时区域 **6 → 11 → 35 → 50** 格（末步并掉余下 13 个红格 → 全盘 63）。

1. 第 2 行第 6 列那块红（6 格：r2c6~c8 + r3c8 + r4c8 + r5c8）染蓝后，立刻接上下方那条 5 格蓝杠（r6c4~c8）→ **11 格**。
2. 染黄：一次接通三片黄 —— 右上 9 格、左侧 9 格、中下 6 格 → **35 格**。
3. 染绿：收掉三片绿 —— 左上 5 格、中间 6 格、左下 4 格 → **50 格**。
4. 染红：50 格染红，自动并掉余下三块红（左侧 6 格、中间 2 格、底部 5 格）→ **63 格全红**。

心法：**每一步只做一件事 —— 选"接下来要吞掉的那一整片"的颜色，再点同一格**。这关的漂亮之处在于：
第 2 步把三片黄一次收完、第 3 步把三片绿一次收完，每步的画笔色都等于"这一步要吸收的颜色"。
坑：起点若选在中间那条**蓝杠**（r6c4~c8）上，第一步刷蓝 = 空操作，颜色顺序必须改成
`红 → 黄 → 绿 → 红` / `黄 → 绿 → 黄 → 红` / `绿 → 黄 → 绿 → 红`（首末同色，各 5 格可用）。

**画盘-8**（目标全蓝 B，限 5 步，最优 5 步）—— 「每步画笔色 = 要吞掉的那一片的颜色」最工整的一关
```
RGYBRGBRG / RGYBRBRRB / RGYBBBRBG / RGYBRBRRG / RGBYRBRRG / RBYYRBRRG / GGYYRBRRG  目标 B
```
17 个连通块、11 个非目标色块。最短 **5 步**（4 步无解），与游戏「剩余步数 5」一致。
同格法 **72 条解、覆盖 29 个格、8 种颜色顺序**（按可点格数降序）：

| 颜色顺序 | 可点格数 | 起点落在哪个初始块 |
|---|---|---|
| 蓝 → 黄 → 绿 → 红 → 蓝 | **12** | 右侧那排红格（r1c8 + r2c7~c8 + r3c7 + r4c7~c8 + r5c7~c8 + r6c7~c8 + r7c7~c8） |
| 黄 → 蓝 → 红 → 绿 → 蓝 | **12** | 中间那块 11 格蓝（r1c4 起）+ 孤立蓝格 (5,3) |
| 红 → 黄 → 绿 → 红 → 蓝 | 11 | 中间 11 格蓝块 |
| 黄 → 红 → 绿 → 红 → 蓝 | 11 | 中间 11 格蓝块 |
| 黄 → 绿 → 红 → 绿 → 蓝 | 11 | 中间 11 格蓝块 |
| 蓝 → 红 → 黄 → 绿 → 蓝 | 5 | 左下那块 5 格黄（r5c4 + r6c3~c4 + r7c3~c4） |
| 蓝 → 红 → 绿 → 黄 → 蓝 | 5 | 同上 |
| 蓝 → 黄 → 红 → 绿 → 蓝 | 5 | 同上 |

推荐：**一直点第 1 行第 8 列**（右上角那格红格），蓝 → 黄 → 绿 → 红 → 蓝，
每步动手时区域 **12 → 26 → 35 → 49 → 61** 格，**雪球单调增长**（这是它比另一条 12 格路线好的原因：
黄→蓝→红→绿→蓝 是 11 → 20 → 22 → 46 → 60，第 3 步几乎不长）。

1. 第 1 步选**蓝**，点右上那格红：把右侧那排 12 格红整体染蓝，**立刻和旁边两个孤立蓝格 (2,9)、(3,8)
   连成一条竖蓝杠** → 区域 **12 格**，全盘蓝 16 → 28。
2. 第 2 步选**黄**：一口吞掉整片黄（右上 9 格 + 中间 6 格 + 左中 11 格）→ **26 格**。
3. 第 3 步选**绿**：吞掉整片绿（左上 5 格 + 右上 9 格 + 右下 2 格 + 第 7 行左侧 2 格 + …）→ **35 格**。
4. 第 4 步选**红**：吞掉整片红（底部 6 格 + 中间 4 格 + 左上 1 格 + 右侧 1 格）→ **49 格**。
5. 第 5 步选**蓝**：区域 **61 格** 变蓝，加上从未被卷入的 2 个原生蓝格 (5,3)、(6,2) → **全盘 63 格全蓝**。

心法：第 1 步「**把枢纽刷成邻居的颜色**」（和第 6、7 关同款）—— 点的是红格却选蓝，
为的是让右排红格和两个孤立蓝格先连成一片；之后每步的画笔色，**正好等于这一步要吸收的那一整片的颜色**。

**画盘-9**（目标全蓝 B，限 6 步，最优 4 步）
```
BYGBBGGRR / BYGYBGGRY / BYGYBGGRY / BYGYBRBRY / BYGYBRBRY / BYGYRRBBY / BYGYRRBBY  目标 B
```
10 个连通块，非蓝块 7 个。最短 4 步，最短解 2 条。
同格法解 6 条、覆盖 6 个格（其实同属一个初始块）：
(1,4)(1,5)(2,5)(3,5)(4,5)(5,5) —— 一路点它，**绿 → 红 → 黄 → 蓝**，每步染色 6→19→31→50。

**画盘-10**（目标全红 R，限 4 步，最优 4 步）
```
RGGGBBBBB / RYYRRRRRR / RYBBBGGGR / RYBBBBBGY / RYBBBBBRY / RYYYRRRRY / RRBBBBBBB  目标 R
```
10 个连通块，非红块 7 个。最短 **4 步**（3 步无解，与「剩余步数 4」一致）。
非同一格的 4 步解只有 4 条；**同格法 33 条、覆盖 19 个格、3 种颜色顺序**：

| 颜色顺序 | 可用点击格数 | 点击格 |
|---|---|---|
| 黄 → 绿 → 蓝 → 红 | 19 | (2,4)…(2,9)、(3,9)、(5,8)、(6,5)…(6,8)、(7,3)…(7,9) |
| 黄 → 蓝 → 绿 → 红 | 7 | (2,4)…(2,9)、(3,9) |
| 绿 → 黄 → 蓝 → 红 | 7 | (2,4)…(2,9)、(3,9) |

推荐（流水线 `recommend()` 现取这条）：「**一直点第 2 行第 4~9 列**（右侧那排红格，含 (3,9)）」：
黄 → 绿 → 蓝 → 红，每步 **7 → 18 → 25 → 50**（末步区域 50 格，是同宽容度下最大的）。
等价备选「一直点第 7 行第 3~9 列（最底下那排蓝格）」：黄 → 绿 → 蓝 → 红，每步 7 → 18 → 25 → 43。

**画盘-11**（目标全红 R，限 5 步，最优 5 步）
```
BBRRRRRYY / BGGGBBBRR / BGYRRRRYR / BRYGGGGYR / GRYYYYGYR / GRRRRRYYR / GGYYYBBBB  目标 R
```
14 个连通块，非红块 10 个。最短 **5 步**（与「剩余步数 5」一致），最短解 15 条。
同格法 87 条、覆盖 21 个格、6 种颜色顺序，**开局和收尾都必须是红**：

| 颜色顺序 | 可用点击格数 |
|---|---|
| 红 → 蓝 → 黄 → 绿 → 红 | 21（最宽容） |
| 红 → 黄 → 绿 → 蓝 → 红 | 18 |
| 红 → 黄 → 蓝 → 绿 → 红 | 18 |
| 红 → 蓝 → 绿 → 黄 → 红 | 12 |
| 红 → 绿 → 黄 → 蓝 → 红 | 9 |
| 红 → 绿 → 蓝 → 黄 → 红 | 9 |

推荐（流水线 `recommend()` 现取这条）：「**一直点第 3 行第 8 列**（右侧那块黄格）」：
红 → 蓝 → 黄 → 绿 → 红，每步 **5 → 22 → 34 → 45 → 58**（末步区域 58 格，最大）。
等价备选「一直点第 1 行第 1 列（左上角蓝格）」：红 → 蓝 → 黄 → 绿 → 红，每步 5 → 17 → 24 → 40 → 53。

**画盘-12**（目标全绿 G，限 5 步，最优 5 步）
```
BBYYYYYRR / YBBYYYRRY / YYBBGRRYY / YYYGRBYYY / YYRRYBBYY / YRRYYYBBY / RRYYYYYBB  目标 G
```
11 个连通块。开局**只有 2 个绿格**（3,5）和（4,4），且互不相邻。最短 **5 步**（与「剩余步数 5」一致），
最短解 20 条。同格法 67 条、覆盖 45 个格、10 种颜色顺序 —— 这关特别宽容。

| 颜色顺序 | 可用点击格数 | 备注 |
|---|---|---|
| 黄 → 蓝 → 黄 → 红 → 绿 | 12（最宽容） | 分右上、左下两处各 6 格 |
| 红 → 蓝 → 黄 → 红 → 绿 | 10 | 含两个绿格 (4,4)、(3,5) |
| 蓝 → 红 → 黄 → 蓝 → 绿 | 9 | 右下一片 |
| 绿 → 红 → 黄 → 蓝 → 绿 | 7 | 左上斜带 |
| 绿 → 红 → 蓝 → 黄 → 绿 | 7 | 左上斜带 |
| 绿 → 蓝 → 黄 → 红 → 绿 | 7 | 右上斜带 + (4,5) |
| 黄 → 红 → 黄 → 蓝 → 绿 | 7 | 右下 |
| 黄 → 蓝 → 红 → 黄 → 绿 | 6 | 左下 |
| 红 → 蓝 → 红 → 黄 → 绿 | 1 | 仅 (4,4) |
| 绿 → 蓝 → 红 → 黄 → 绿 | 1 | 仅 (4,5) |

推荐「**一直点第 1 行第 8 列**（右上角那块红格）」：黄 → 蓝 → 黄 → 红 → 绿，每步 6 → 23 → 36 → 54 → 61。
同样顺序从「第 5 行第 3 列（左下那块红格）」点也一样通关，每步 6 → 24 → 37 → 54 → 61。

**画盘-13**（目标全绿 G，限 5 步，最优 5 步）—— 2026-09-19 新增，棋盘来自用户截图自动标定
```
GGGYYYYYY / RRRBBBBBY / GGGGGRRRR / YYBBBBBRR / RRRRYYYYY / BBBGGGBBY / BBBBRRRRR  目标 G
```
14 个连通块。最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 3 行第 1 列**」（该初始连通块共 5 格 = r3c1~c5，**块内点哪个都一样**）：
**蓝 → 黄 → 红 → 蓝 → 绿**，每步 5 → 15 → 30 → 48 → 57 格。
宽容度：本关共 **22 个可点格**（分布在 4 个初始连通块），本条颜色顺序下 11 个。

1. 第 1 步**又是「反着染」**——点的是 **5 格绿块**却选【蓝】。这块绿上下正好各贴一片蓝
   （第 2 行 c4~c8、第 4 行 c3~c7），一步就把三片并成 **15 格**。
2. 第 2 步选【黄】：这块一次够到三片黄（顶部 r1c4~c9+r2c9、左侧 r4c1~c2、中下 r5c5~c9+r6c9）→ **30 格**。
3. 第 3 步选【红】：把四片红（左上 r2c1~c3、右上 r3c6~c9+r4c8~c9、左中 r5c1~c4、底部 r7c5~c9）一次收完 → **48 格**。
4. 第 4 步选【蓝】：再接上两片蓝 —— 底部那条 7 格（r6c1~c3 + r7c1~c4）和右侧 r6c7~c8 那 2 格 → **57 格**。
5. 第 5 步选【绿】：末步把左上 3 格绿（r1c1~c3）与中下 3 格绿（r6c4~c6）一并收进来 → **全盘 63 格全绿**。

心法：和第 4、5、8 关同款 —— **目标色的块不是"已完成"，而是必须亲手拆掉的枢纽**。
这关起点那 5 格绿就是枢纽：它上下夹着两片蓝，先把它刷蓝才能把蓝片抢先连成一片大的；
之后每一步的画笔色，**正好等于这一步要吸收的那一整片的颜色**（蓝→黄→红→蓝）。
坑：起点若选在**本来就是绿色**的其他块上（r1c1~c3 或 r6c4~c6），第一步刷绿 = 空操作，
颜色顺序必须改掉；这关 22 个可点格里有 11 个能走推荐那条顺序。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level13.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 2），成品图 `assets/levels/画盘13_通关路线.png`。
> 三道校验（双实现对账 5 步 / 22 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。

**画盘-14**（目标全蓝 B，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
GYBRRGGYB / GYBRYBRYB / GYBRYBRYR / GYBGYBRYR / RYBGYBRBR / RRBGYYRBR / RRBGYYBBR  目标 B
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 1 行第 3 列**」—— 也就是第 3 列那条 **7 格竖蓝**（r1c3~r7c3，块内点哪个都一样）：
**红 → 绿 → 黄 → 红 → 蓝**，每步 7 → 16 → 26 → 43 → 53 格。
宽容度：**本关只有 7 个可点格，而且全在这块里**（1~19 关里最窄的一关；第 20、21 关更窄）—— 起点只能落在这条竖蓝上，
点别处等于没点，没有第二条能通关的走法。

1. 第 1 步**必须「反着染」**——把这条 7 格**目标色**蓝块刷成【红】，它立刻与右邻红
   （r1c4、r1c5、r2c4、r3c4）和左下红（r5c1、r6c1~c2、r7c1~c2）连成 **16 格**。
2. 第 2 步【绿】：一口气吸收三片绿 —— 左边缘 c1 的 r1~r4（4 格）、中段竖排 c4 的 r4~r7（4 格）、
   右上 r1c6~c7（2 格）→ **26 格**。
3. 第 3 步【黄】：再收三片黄 —— c2 竖排 r1~r5（5 格）、中部 8 格（c5 的 r2~r7 + r6c6、r7c6）、
   右缘 c8 的 r1~r4（4 格）→ **43 格**。
4. 第 4 步【红】：收掉两片红 —— 右缘 c9 竖排 r3~r7（5 格）、c7 竖排 r2~r6（5 格）→ **53 格**。
5. 第 5 步【蓝】：剩下的 10 格本来全是蓝 —— 右上 2 格（r1c9、r2c9）、中列 c6 的 r2~r5（4 格）、
   右下 4 格（r5c8、r6c8、r7c7~c8），末步一并 → **全盘 63 格全蓝**。

心法：**目标色的整块不是"已完成"，而是必须先亲手拆掉的枢纽**（第 13 关同款思路，这关更极端）——
那条 7 格竖蓝正是唯一的突破口，先刷红把它切开；之后每一步的画笔色，**正好等于这一步要吸收的那一整片颜色**。
坑：这关没有备选起点，别指望"换一格点点看"，只能按这条顺序走。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level14.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 3），成品图 `assets/levels/画盘14_通关路线.png`。
> 三道校验（双实现对账 5 步 / 7 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。

**画盘-15**（目标全蓝 B，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
GGGRRYYGY / YBBBRRYGY / YBYBYYGGY / YYYBYRRRY / GBGGYYRRB / GBBGGBBBB / GBBGYYYYY  目标 B
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 2 行第 2 列**」—— 这块是 **6 格十字形**（r2c2~c4、r3c2、r3c4、r4c4，块内点哪个都一样）：
**黄 → 绿 → 红 → 黄 → 蓝**，每步 6 → 17 → 32 → 41 → 53 格。
宽容度：本关 16 个可点格、分布在 3 个初始连通块；本条颜色顺序下 11 个格子可用（比第 14 关宽松，但也别乱点）。

1. 第 1 步又是**「反着染」**——起点那块是 **6 格蓝**（目标色）却要选【黄】，一步就并进两片黄：
   左中 6 格（r2c1、r3c1、r3c3、r4c1~c3）与中部 5 格（r3c5~c6、r4c5、r5c5~c6）→ **17 格**。
2. 第 2 步【绿】：一口气并进 **4 片绿** —— 左上 r1c1~c3（3 格）、右上 r1c8 + r2c8 + r3c7~c8（4 格）、
   左下 c1 的 r5~r7（3 格）、中下 r5c3~c4 + r6c4~c5 + r7c4（5 格）→ **32 格**。
3. 第 3 步【红】：并进两片红 —— 顶部 r1c4~c5、r2c5~c6（4 格）与右中 r4c6~c8、r5c7~c8（5 格）→ **41 格**。
4. 第 4 步【黄】：并进三片黄 —— 顶部 r1c6~c7 + r2c7（3 格）、右缘 c9 的 r1~r4（4 格）、底部 r7c5~c9（5 格）→ **53 格**。
5. 第 5 步【蓝】：最后并进左下 5 格（r5c2、r6c2~c3、r7c2~c3）与右下 5 格（r5c9、r6c6~c9）→ **全盘 63 格全蓝**。

心法：还是那句 —— **目标色的块不是"已完成"，而是必须先亲手拆掉的枢纽**（第 13、14 关同款）。
这关的目标色蓝一共 16 格、被切成 4 片，其中一片正好就是起点；先把这 6 格刷黄拆开，
此后每一步的画笔色都等于"这一步要吸收的那一整片颜色"（黄 → 绿 → 红 → 黄）。
注意第 1 步与第 4 步都是【黄】——**同一种颜色在顺序里出现两次是正常的**，不是笔误（第 12 关也是）。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level15.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 5），成品图 `assets/levels/画盘15_通关路线.png`。
> 三道校验（双实现对账 5 步 / 16 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片，由 `tools/step_detail.py 15` 现算核对（别靠肉眼数）。

**画盘-16**（目标全蓝 B，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
RRRGGRRRR / RRRGGRRRR / YBYBYBYBY / BYBYBYBYB / RRRGGRRRR / BBBGGBBBB / RRRGGRRRR  目标 B
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 3 行第 5 列**」—— 这是个 **1 格黄点**（r3c5）：
**蓝 → 绿 → 红 → 黄 → 蓝**，每步 1 → 4 → 14 → 42 → 50 格。
宽容度：本关**只有 4 个可点格**（全在第 3、4 行的黄蓝交错带里：r3c4、r3c5、r4c4、r4c5），
其中 r3c5 与 r4c4 这 2 格能走出上面这条顺序；另 2 格（r3c4、r4c5）走「黄 → 绿 → 红 → 黄 → 蓝」同样 5 步通关。

1. 第 1 步【蓝】：把起点那个**黄点刷成蓝**，它立刻与左、右、下三个蓝点（r3c4、r3c6、r4c5）连成 **4 格**。
2. 第 2 步【绿】：并进上下两片绿 —— 上方 r1c4~c5 + r2c4~c5（4 格）、下方 r5c4~c5 + r6c4~c5 + r7c4~c5（6 格）→ **14 格**。
3. 第 3 步【红】：一口气并进 **6 片红** —— 左上 6 格、右上 8 格、中左 3 格、中右 4 格、底左 3 格、底右 4 格（共 28 格）→ **42 格**。
4. 第 4 步【黄】：把散在第 3/4 行的 **8 个孤立黄点**（r3c1、r3c3、r3c7、r3c9、r4c2、r4c4、r4c6、r4c8）一次收干净 → **50 格**。
5. 第 5 步【蓝】：最后收掉剩下的 6 个蓝点（r3c2、r3c8、r4c1、r4c3、r4c7、r4c9）加上第 6 行两片蓝（r6c1~c3、r6c6~c9）→ **全盘 63 格全蓝**。

心法：这关盘面是「**横条纹 + 中间两行棋盘格**」——第 3、4 行是黄蓝交错的孤立点阵，其余 5 行是「红框夹两列绿」。
破法是**先在这两行的点阵上立住脚**（起手只能是那 4 个点之一），再用「绿 → 红」把上下 5 行整片吞掉，
最后用「黄 → 蓝」把点阵里剩下的黄点、蓝点各扫一遍。
注意这关**第 1 步与第 5 步都是【蓝】**，而起点只有 **1 格** —— 块小不等于没用：
它四面正好各贴一个蓝点，刷蓝就能一次把 4 个点锁成一片。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level16.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 6），成品图 `assets/levels/画盘16_通关路线.png`。
> 三道校验（双实现对账 5 步 / 4 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片、以及全部可用起点，均由 `tools/step_detail.py 16` 现算核对（别靠肉眼数）。

**画盘-17**（目标全黄 Y，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
GGGYYYYYY / YYBBBBBBY / YGGRRRRYB / YRYYYYBYB / YRBBBBBGB / YRRRRGGGB / BBBBBRRRB  目标 Y
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 2 行第 1 列**」—— 这块是 **6 格**（左上竖条 r2c1、r3c1、r4c1、r5c1、r6c1 加 r2c2）：
**蓝 → 红 → 蓝 → 绿 → 黄**，每步 6 → 17 → 30 → 41 → 50 格。
宽容度：本关 **41 个可点格、8 个起点块**（1~17 关里最宽松的一关），本条颜色顺序下 18 个格子可用；
换别的块起手也能 5 步通关，但颜色顺序要跟着换（例如从第 2 行那块 6 格蓝起手就是「黄 → 绿 → 蓝 → 红 → 黄」）。

1. 第 1 步**「反着染」**——起点是 6 格**黄**（目标色）却要选【蓝】：一步并进第 2 行那条 6 格蓝（r2c3~c8）
   与第 7 行左侧 5 格蓝（r7c1~c5）→ **17 格**。
2. 第 2 步【红】：并进三片红 —— 上部 r3c4~c7（4 格）、左侧 L 形 r4c2 + r5c2 + r6c2~c5（6 格）、底部 r7c6~c8（3 格）→ **30 格**。
3. 第 3 步【蓝】：并进两片蓝 —— 右缘 c9 的 r3~r7（5 格）与中部 r4c7 + r5c3~c7（6 格）→ **41 格**。
4. 第 4 步【绿】：并进三片绿 —— 左上 r1c1~c3（3 格）、上部 r3c2~c3（2 格）、右下 r5c8 + r6c6~c8（4 格）→ **50 格**。
5. 第 5 步【黄】：末步并进三片黄 —— 顶部 r1c4~c9 + r2c9（7 格）、右侧 r3c8 + r4c8（2 格）、中部 r4c3~c6（4 格）→ **全盘 63 格全黄**。

心法：这关是 1~17 关里**最宽松**的一关（41 个可点格），但「目标色先亲手拆掉」这条规律照旧成立 ——
起点那 6 格黄本身就是目标色，第 1 步却要用【蓝】把它切开；之后「红 → 蓝 → 绿」逐层收，
最后一步【黄】再把散落的黄片全部并回来。注意**蓝在第 1、3 步各出现一次**（同一颜色重复出现是正常的）。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level17.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 7），成品图 `assets/levels/画盘17_通关路线.png`。
> 三道校验（双实现对账 5 步 / 41 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片、以及全部 8 个可用起点块，均由 `tools/step_detail.py 17` 现算核对。

**画盘-18**（目标全黄 Y，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
YRRRBBBBB / YGGGGGGGR / YRBBBYYYR / YRBGGGGYR / YRYYYYGYR / YBBBBBYYB / GGGGRRRRB  目标 Y
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 6 行第 2 列**」—— 这块是 **5 格**（r6c2~c6 那条横蓝，块内点哪个都一样）：
**黄 → 绿 → 红 → 蓝 → 黄**，每步 5 → 22 → 38 → 52 → 63 格。
宽容度：**40 个可点格、8 个起点块**都能 5 步通关（推荐这块自己还有 4 条等价顺序），本条顺序下 19 个格子可用。

1. 第 1 步【黄】：起点是 5 格**蓝**（不是目标色），选目标色【黄】一步并进三片黄 ——
   左竖条 r1c1~r6c1（6 格）、右侧 r3c6~c8 + r4c8 + r5c8 + r6c7~c8（7 格）、中部 r5c3~c6（4 格）→ **22 格**。
2. 第 2 步【绿】：并进三片绿 —— 第 2 行 r2c2~c8（7 格）、中部 r4c4~c7 + r5c7（5 格）、底部 r7c1~c4（4 格）→ **38 格**。
3. 第 3 步【红】：并进四片红 —— 顶部 r1c2~c4（3 格）、右缘 c9 的 r2~r5（4 格）、左中 r3c2~r5c2（3 格）、
   底部 r7c5~c8（4 格）→ **52 格**。
4. 第 4 步【蓝】：并进最后三片蓝 —— 顶部 r1c5~c9（5 格）、中部 r3c3~c5 + r4c3（4 格）、右下 r6c9 + r7c9（2 格）
   → 全盘 **63 格此时已连成一片**（颜色是蓝）。
5. 第 5 步【黄】：把这一整片刷成黄 —— **末步不再并片，只是上色** → **全盘 63 格全黄**。

心法：这关起点是蓝、目标是黄，所以第 1 步**直接选目标色【黄】** —— 把起点刷黄的同时顺手吃掉三片黄（17 格），
一步就把区域从 5 格顶到 22 格。其后「绿 → 红 → 蓝」每层都还是那条规律：**这一步的画笔色 = 要吸收的那一整片的颜色**。
另注意这关的收尾方式：**第 4 步之后全盘已经连成一片，第 5 步只是"上色"**，所以「每步染色」的最后一项就是 63
（第 16 关那种"末步才并片"是另一种收尾，两种都正常）。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level18.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 8），成品图 `assets/levels/画盘18_通关路线.png`。
> 三道校验（双实现对账 5 步 / 40 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片、以及全部 8 个可用起点块（含各自全部可行顺序），均由 `tools/step_detail.py 18` 现算核对。

**画盘-19**（目标全蓝 B，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
BBBBBBYYY / YYYRRRYYY / YYGGGGGGB / GRRRRBBBB / GGYYYYYYR / RRRRRBBRR / RBBBBGGGG  目标 B
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 1 行第 7 列**」—— 这块是 **6 格**（右上角 r1c7~c9 + r2c7~c9，块内点哪个都一样）：
**蓝 → 黄 → 红 → 绿 → 蓝**，每步 6 → 17 → 28 → 44 → 57 格。
宽容度：35 个可点格、7 个起点块都能 5 步通关，本条顺序下 16 个格子可用
（推荐这块**只有这 1 条**顺序，别的起点块往往有多条）。

1. 第 1 步**「反着染」**——起点是 6 格**黄**却选目标色【蓝】：一步并进左上那条 6 格蓝（r1c1~c6）
   与右下 5 格蓝（r3c9、r4c6~c9）→ **17 格**。
2. 第 2 步【黄】：并进两片黄 —— 左上 r2c1~c3 + r3c1~c2（5 格）、中右 r5c3~c8（6 格）→ **28 格**。
3. 第 3 步【红】：并进四片红 —— 上部 r2c4~c6（3 格）、中左 r4c2~c5（4 格）、右下 r5c9 + r6c8~c9（3 格）、
   左下 r6c1~c5 + r7c1（6 格）→ **44 格**。
4. 第 4 步【绿】：并进三片绿 —— 中上 r3c3~c8（6 格）、左缘 r4c1 + r5c1~c2（3 格）、底部 r7c6~c9（4 格）→ **57 格**。
5. 第 5 步【蓝】：最后并进两片蓝 —— r6c6~c7（2 格）与 r7c2~c5（4 格）→ **全盘 63 格全蓝**。

心法：起手把右上角那块黄刷成目标色【蓝】，顺手吃掉两片蓝（11 格），一步就从 6 格顶到 17 格；
之后「黄 → 红 → 绿」每层都还是"这一步的画笔色 = 要吸收的那一整片的颜色"，
最后一步【蓝】把剩下的两片蓝收回来 —— 注意**第 1 步与第 5 步同为【蓝】**（同色重复出现是正常的）。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level19.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 9），成品图 `assets/levels/画盘19_通关路线.png`。
> 三道校验（双实现对账 5 步 / 35 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片、以及全部 7 个可用起点块，均由 `tools/step_detail.py 19` 现算核对。

**画盘-20**（目标全红 R，限 5 步，最优 5 步）—— 2026-09-20 新增，棋盘来自用户截图自动标定
```
YYYYGGRRY / RRGGBBRRR / GGBBGGBBR / RRYYRRRRR / BBGGBBGGR / RRGGBBRRR / YYYYGGRRY  目标 R
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 4 行第 3 列**」—— 这块是 **2 格**（r4c3~c4 那对黄格，两格点哪个都一样）：
**红 → 蓝 → 绿 → 黄 → 红**，每步 2 → 21 → 33 → 49 → 59 格。
宽容度：**全关只有 2 个可点格，而且就是这 2 格**（可用起点块 1 个、可行顺序 1 条）——
**第 21 关之前最窄的一关：除了这两格，点哪里都没有反应**（第 21 关只有 1 格，更窄）。

1. 第 1 步【红】：把这对黄格刷红，一步并进右侧那块 **17 格红**（r1c7~c8、r2c7~c9、r3c9、r4c5~c9、
   r5c9、r6c7~c9、r7c7~c8）与中左 r4c1~c2（2 格）→ **21 格**。
2. 第 2 步【蓝】：并进 5 片蓝 —— r2c5~c6、r3c3~c4、r3c7~c8、r5c1~c2 各 2 格，加 r5c5~c6 + r6c5~c6 那 4 格 → **33 格**。
3. 第 3 步【绿】：并进 7 片绿 —— r1c5~c6、r2c3~c4、r3c1~c2、r3c5~c6、r5c7~c8、r7c5~c6 各 2 格，
   加 r5c3~c4 + r6c3~c4 那 4 格 → **49 格**。
4. 第 4 步【黄】：并进 4 片黄 —— 顶行 r1c1~c4（4 格）、r1c9（1 格）、底行 r7c1~c4（4 格）、r7c9（1 格）→ **59 格**。
5. 第 5 步【红】：收掉最后两片红（r2c1~c2、r6c1~c2 各 2 格）→ **全盘 63 格全红**。

心法：这关盘面是**上下完全对称的重复花纹**（第 1 行 = 第 7 行、第 2 行 = 第 6 行、第 3 行 = 第 5 行），
整盘只有第 4 行中间那 2 格黄是"孤岛"——它左右都贴着红，所以第 1 步【红】就能把右侧 17 格红一次吞下，
一局从 2 格直接跳到 21 格。之后「蓝 → 绿 → 黄」每层都是把同色的 2 格小片、4 格小片挨个吃掉，最后一步【红】收尾。
这关最值得记的是**宽容度 = 0**：可点格只有起点那 2 格，没有第二种打法。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level20.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 10），成品图 `assets/levels/画盘20_通关路线.png`。
> 三道校验（双实现对账 5 步 / 2 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片由 `tools/step_detail.py 20` 现算核对（这关可用起点块只有 1 个）。

**画盘-21**（目标全红 R，限 5 步，最优 5 步）—— 2026-09-21 新增，棋盘来自用户截图自动标定
```
GRGGGGGRG / GGRGYGRGG / GBGRGYGYG / RBYGYGYYY / GBGYGRGYG / GGRGYGRGG / GRGGGGGRG  目标 R
```
最短 **5 步**（与「剩余步数 5」一致，4 步无解）。
推荐「**一直点第 4 行第 5 列**」—— 这是 **1 格黄点**（r4c5）：
**绿 → 黄 → 蓝 → 绿 → 红**，每步 1 → 5 → 15 → 18 → 52 格。
宽容度：**全关只有 1 个可点格**（可用起点块 1 个、可行顺序 1 条）—— **1~21 关里最窄的一关**，
除了 r4c5 这一格，点任何地方都不会有反应。

1. 第 1 步【绿】：把起点那格黄刷绿，立刻与上下左右四个绿点（r3c5、r4c4、r4c6、r5c5）连成 **5 格**十字。
2. 第 2 步【黄】：并进六片黄 —— r2c5、r3c6、r4c3、r5c4、r6c5 各 1 格，加右中那片 5 格
   （r3c8 + r4c7~c9 + r5c8）→ **15 格**。
3. 第 3 步【蓝】：并进唯一的蓝片 —— 左侧 c2 的 r3~r5（3 格）→ **18 格**。
4. 第 4 步【绿】：一口气并进 **10 片绿**（4 + 7 + 4 + 1 + 1 + 4 + 1 + 1 + 4 + 7 = 34 格）→ **52 格**。
5. 第 5 步【红】：最后把盘上剩下的 **11 个孤立红点**（r1c2、r1c8、r2c3、r2c7、r3c4、r4c1、r5c6、r6c3、r6c7、r7c2、r7c8）
   一次收干净 → **全盘 63 格全红**。

心法：这关和第 20 关一样是**上下对称花纹**（r1 = r7、r2 = r6、r3 = r5），但目标色红被切得很碎 ——
11 个红点全是孤立单格。所以前四步都在"攒面积"：先用【绿】把自己融进背景绿，再用「黄 → 蓝」吃掉黄、蓝，
第 4 步用【绿】一次并进 10 片绿把区域顶到 52 格，最后一步【红】把 11 个红点全部收回。
**起点只有一格、别无选择**，这是 1~21 关里宽容度最低的一关。

> 本关数据由 `tools/build_level_index.py` 从 `examples/board_level21.txt` 现算并写回
> `assets/levels/levels.json`（索引 version 11），成品图 `assets/levels/画盘21_通关路线.png`。
> 三道校验（双实现对账 5 步 / 1 可点格、最短性证明≤4 步无解、成品反查 PASS）全过，取色 63/63。
> 每一步并了哪几片由 `tools/step_detail.py 21` 现算核对（这关可用起点块只有 1 个）。

## 跨平台 / 环境

- 依赖只有 Pillow，Python ≥ 3.8；Windows / macOS / Linux 都跑过。
- 脚本自己 `open(path, "w", encoding="utf-8")` 写盘，报告一律带 UTF-8 BOM **不要**用系统的
  重定向去写（Windows PowerShell 5.1 的 `Set-Content -Encoding UTF8` 会写 BOM）。
  三个求解脚本读 board 文件时用 `utf-8-sig`，所以两种都容错，但别主动引入 BOM。
- 截图一般是 1920×1080；很多 agent 的图片查看是**缩略图**，**别用缩略图上的坐标**，
  一切以脚本扫出来的像素坐标为准。
- `board_tool.py auto` 只写 `grid_report.txt`，不直接产出 board 文件；board 文件是「把 `GRID:` 段抄出来 +
  末尾补一行目标色」，`pipeline.py` 会自动落一份到 `<work>/board<level>.txt`。
- **本机（开发机）专用事项已挪到 `references/dev-machine-notes.md`**（WorkBuddy 工具的 stdout 行为、
  托管 Python 路径、safe-delete 钩子等）—— 在别的机器上用不需要看那篇。

## 反模式

- **只信一个脚本**。两个实现必须互相对账（步数 + 可用格总数），本项目靠这个抓到过两次漏报。
- 只靠肉眼数格子/看颜色 —— 9 列很容易数成 10 列，取色必须程序化 + 重建图核对。
- 看到"非目标色块 7 个 > 剩余 4 步"就断言无解 —— 正确反应是去试同格法。
- 用 DFS / IDDFS + 全局 visited —— 会误剪掉最短路径（本项目踩过），**必须逐层 BFS + 去重**。
- 用整盘字符串当 BFS 状态跑到深度 5 —— 状态数爆炸（第 11/12 关实测跑不出来），改用组件图状态。
- 修 `board_tool.py` 时把"向四周扩展"去掉、退回"数候选段当列数" —— 最右一列必漏。
- 修标定判据时**假设"缝隙一定是近白留白"**（v3 的写法）—— 第 1~3 关的缝是**米色 (207,174,145)**，
  这一条会把所有候选段全否掉、标定直接失败。判据只能建立在"棋盘是一串等间距的瓦片色格"上。
- 用「**合并窄缝**」去处理瓦片内的高光带 —— 缝里散落的 1px 杂点会顺着合并把两道真缝桥接起来，
  整行并成一格（第 3 关实测 x=792 处 4 个 1px 杂点桥掉了 17px 的真缝）。正确做法是丢碎段 + 容忍多出来的段。
- 拿**缩略图**判断盘面颜色或数格子 —— 实测被坑过三次：把红读成黄、把正确的蓝圈看成红圈、
  把"第 1 行全黄"看成"红块从第 1 行开始"。要么走脚本取色，要么把那一处**裁出来放大**再看。
- 把角色立绘、卡片、按钮附近的色块当成棋盘格 —— 判断依据要带"等间距"这一条。
- **按直觉先去处理"离目标色最远"的小色块**（比如第 4 关先刷那 4 条蓝条）—— 3 步绝对打不完。
  正确做法是先找「同时贴住最多异色碎片的那个连通块」，第一步**反着把它刷成碎片的颜色**，
  让碎片先连成一大块，后面每步再"吸收"贴边的新色块。
- **把"已经是目标色"的块当成已完成**（第 5 关就是这么设计的）。第 4、5 两关的最优解第一步都是
  「点一个**目标色**格、却刷成**别的**颜色」。目标色的块往往正是那个必须亲手拆掉的枢纽，
  别下意识跳过它；也**不要用"非目标色块数 vs 剩余步数"来判断难度**。
- **把「每步染色格数」末项当成终局格数** —— 它是**本次点击实际改色的格数**，不加本来就同色的块。
  第 5 关末步只染 56 格但全盘 63 格变红（r7c3~c9 那 7 格本来就红）；第 8 关末步 61 格但全盘 63 格全蓝
  （(5,3)、(6,2) 两个原生蓝格从未被卷入）。这是正常的。
- 给 `verify_render.py` 之类工具**硬编码版面纵坐标** —— 棋盘上边缘会随顶部标题/副标题折行数变化
  （实测偏 10px，只因格子有 42px 宽才侥幸采样正确）。要**从图里扫出来**，不要写死。
- 写面板定位逻辑时假设"**每列都有 nrows 个面板**" —— 最后一列常常少一个（第 8 关 5 步 3 列 = 5 个面板，
  第 3 列只有 1 个）。期望数要按 `k % ncols` 逐列算。
- **自己临时写渲染脚本排版交付图** —— 版式已经定死（见 §3），一律走 `render_delivery.py`。
- **把 `画盘-N` 标签去掉、或把底部那行「注意：非目标色块…」加回来** —— 两个方向都被用户明确否过：
  标签要**保留并放大**（深色胶囊 + 白字，26 号），底部说明**默认不画**（平时不要传 `--foot`）。
- 把 `solve_route.png`（求解器自带的黑圈图）或 `grid_recon.png`（原图 vs 重建核对图）当交付物发给用户。
- 出完图不 Read 一遍就交付 —— 换行截断、圈跑偏这些只有看图才能发现。
- **逐条手动跑脚本** —— 单关 6~8 轮"等输出再决定下一步"，实测 5~10 分钟，而脚本本身才 2 秒。
  一律走 `pipeline.py`。同理：**想提速先量再改**：本项目实测发现瓶颈是往返次数而不是算力
  （脚本合计 20 秒 / 总耗时 5~10 分钟），如果一上来就去优化 BFS 就找错方向了。
- **把"跑脚本"和"看结论"拆成两次调用** —— 若你的执行工具不回显 stdout，可以接受；
  但大多数工具会回显，那就一条 `... && cat <summary>` 拿全，别平白多一次往返。
- **改 `recommend()` 的打分规则却不做全量回归** —— 它会静默换掉推荐路线（第 8 关就因此
  从 12→26→35→49→61 掉到 11→20→22→46→60 那条"第 3 步几乎不长"的）。改完必跑
  9 关回归，逐条比对步数 / 可点格数 / 颜色顺序 / 推荐格 / 每步格数。
- **发现副标题被右边缘截断，就把文案写短去迁就脚本** —— 截断是渲染器的 bug（早期是单行绘制），
  已改为按画布宽度自动折行；正确的修法是改脚本，不是砍文案。
- **只靠 Read 缩略图判断颜色画对了没有** —— 缩略图下红/黄会看错（第 6 关第 3 帧实测把红读成黄，差点误判渲染出错；
  第 8 关第 1 帧实测把**蓝圈看成红圈**，因为圈底下的格子是红的）。颜色/矩阵这类事一律交给
  `verify_render.py` 做像素级反查，肉眼只负责看排版；真要围观一处细节，就把那处**裁出来放大**再看。
